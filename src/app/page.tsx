import Books from '@/container/Books';
import Hero from '@/container/Hero';
import React from 'react';

const page = () => {
    return (
        <div>
            <Hero/>
            <Books/>
        </div>
    );
};

export default page;